name: Email Action Test

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: 21f1004227@ds.study.iitm.ac.in
        run: echo "Hello, world! This action step includes my email."
